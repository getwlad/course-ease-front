import { ParseDateDirective } from './parse-date.directive';

describe('ParseDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ParseDateDirective();
    expect(directive).toBeTruthy();
  });
});
